package com.bolivar.bioingenieria.app.sigma_bb.shared.domain;

/**
 * Marker interface for data returned by domain report providers.
 *
 * It also exposes default accessors that implementations may override to
 * provide common fields (so callers can read them without casting).
 */
public interface ReportData {
}

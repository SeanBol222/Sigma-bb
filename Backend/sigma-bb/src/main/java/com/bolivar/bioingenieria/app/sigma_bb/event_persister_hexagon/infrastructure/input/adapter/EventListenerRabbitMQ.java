package com.bolivar.bioingenieria.app.sigma_bb.event_persister_hexagon.infrastructure.input.adapter;

import org.springframework.stereotype.Component;

@Component
public class EventListenerRabbitMQ {
/*
    private final EventListenerPort port;

    @Autowired
    public EventListenerRabbitMQ(EventListenerPort port) {
        this.port = port;
    }

    @RabbitListener(queues = "events-domain-all-queue", containerFactory = "rabbitListenerContainerFactory")
    public void onEvent(DomainEvent<? extends Payload> event) {
        System.out.println("Rabbit Event: " + event);
        //port.handle(event);
    }*/
}

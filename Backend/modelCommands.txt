opencode run "Genera el modelo de dominio Model con campos id(UUID), name(String), manufacturerId(UUID), equipoId(String). Sin dependencias externas. Utiliza @Data, @Builder, @NoArgsConstructor, @AllArgsConstructor de Lombok."
opencode run "Genera ModelServicePort con metodos CRUD: findAll, findById, save, update, delete usando Model."
opencode run "Genera ModelPersistencePort con los mismos metodos que ModelServicePort."
opencode run "Genera ModelService que implementa ModelServicePort y delega en ModelPersistencePort."
opencode run "Genera ModelRequest con campos name, manufacturer_id, equipo_id tipo String con @NotBlank. Usar @JsonProperty para mapear a camelCase interno. Utiliza @Data, @Builder, @NoArgsConstructor, @AllArgsConstructor de Lombok."
opencode run "Genera ModelResponse con campos id, name, manufacturer_id, equipo_id tipo String. Usar @JsonProperty. Utiliza @Data, @Builder, @NoArgsConstructor, @AllArgsConstructor de Lombok."
opencode run "Genera ModelRestMapper con MapStruct para convertir entre Model, ModelRequest y ModelResponse."
opencode run "Genera ModelController con endpoints CRUD usando ModelServicePort y ModelRestMapper. Sin logica de negocio."
opencode run "Genera ModelEntity JPA para tabla modelo con campos k_id_modelo UUID @Id @GeneratedValue, n_invmia String nullable=false, k_id_fabricante UUID, k_id_equipo String. Usar @Column(name=...) respetando exactamente los nombres de BD. Lombok: @Data @Builder @NoArgsConstructor @AllArgsConstructor."
opencode run "Genera SpringModelRepository extendiendo JpaRepository<ModelEntity, UUID>."
opencode run "Genera ModelPersistenceMapper con MapStruct incluyendo updateEntityFromDomain ignorando nulls y el id."
opencode run "Genera ModelPersistenceAdapter que implementa ModelPersistencePort usando repository y mapper. findById debe lanzar ModelNotFoundException."
opencode run "Genera ModelNotFoundException con mensaje 'Model with id <id> not found'."
opencode run "Genera GlobalHandlerError para Model con: ModelNotFoundException -> 404, MethodArgumentNotValidException -> 400, Exception -> 500. Usar ErrorResponse."